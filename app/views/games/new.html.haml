%h1 Welcome to memoizr!
%hr
%h2.alt This application will kick a$$

= form_for(:game, @game, :url => game_path, :method => :post) do |f|
  = f.label :images_category
  %br
  = f.text_field :images_category
  = @game.errors[:images_category]
  %br
  = f.submit 'Start a new game'
