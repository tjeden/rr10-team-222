<% if !@result and @index2.present? %>
  reveal_image_at_index("<%= flickr_url(@image, :size => :small) %>", <%= @index1 %>, false);
  prepare_to_hide_images_at_indexes(<%= @index1 %>, <%= @index2 %>);
<% else %>
  reveal_image_at_index("<%= flickr_url(@image, :size => :small) %>", <%= @index1 %>, true);
<% end %>